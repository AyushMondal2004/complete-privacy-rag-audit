Use this folder for your own exploratory pilot notebook — e.g. loading 5-10
policies, inspecting cleaned text, checking chunk boundaries by eye, and
sanity-checking a handful of LLM outputs against the YAML ground truth
before committing to full-corpus run parameters. This is genuinely useful
before your Chapter 3 write-up, and it's fast to do in Jupyter.
